<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Customer Login - MyFinBank</title>
<style>
        body { 
            font-family: Arial, sans-serif; 
            background-color: #f4f4f4; /* Light background */
            display: flex; 
            justify-content: center; 
            align-items: center; 
            height: 100vh; 
            margin: 0; 
        }
        .container { 
            background-color: #ffffff; 
            padding: 30px; 
            border-radius: 8px; 
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); 
            width: 100%;
            max-width: 350px; 
            text-align: center; 
            border-top: 5px solid #0056b3; /* Primary Blue accent */
        }
        h1 { 
            color: #333; 
            margin-bottom: 25px; 
            font-size: 24px; 
        }
        input[type="text"], input[type="password"] { 
            width: 100%; 
            padding: 10px; 
            margin-bottom: 15px; 
            border: 1px solid #ccc; 
            border-radius: 4px; 
            box-sizing: border-box; 
            font-size: 16px;
        }
        button { 
            background-color: #0056b3; /* Primary Blue Button */
            color: white; 
            padding: 10px 15px; 
            border: none; 
            border-radius: 4px; 
            cursor: pointer; 
            font-size: 16px; 
            width: 100%; 
            transition: background-color 0.3s;
        }
        button:hover { 
            background-color: #004494; 
        }
        .message { 
            padding: 10px; 
            border-radius: 4px; 
            margin-bottom: 15px; 
            font-weight: bold; 
            text-align: left;
        }
        .error { 
            background-color: #f8d7da; 
            color: #721c24; 
            border: 1px solid #f5c6cb; 
        }
        .success { 
            background-color: #d4edda; 
            color: #155724; 
            border: 1px solid #c3e6cb; 
        }
        .link-text { 
            margin-top: 20px; 
            font-size: 14px; 
        }
        .link-text a { 
            color: #0056b3; 
            text-decoration: none; 
        }
        .link-text a:hover { 
            text-decoration: underline; 
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Customer Login</h1>
        
        <!-- Messages -->
        <div th:if="${error}" class="message error" th:text="${error}">Login Error Message</div>
        <div th:if="${success}" class="message success" th:text="${success}">Success Message</div>

        <form method="POST" th:action="@{/login}" th:object="${customer}">
            <input type="text" th:field="*{username}" placeholder="Username" required>
            <input type="password" th:field="*{password}" placeholder="Password" required>
            <button type="submit">Log In</button>
        </form>
        
        <div class="link-text">
            New Customer? <a th:href="@{/register}">Register Now</a>
        </div>
    </div>
</body>
</html>